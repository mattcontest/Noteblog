import { Card, Icon, Image } from "semantic-ui-react";


export default function PostCard({ notes, isProfile, addLike, removeLike, user}){

    //Check if the user has noted a specific Note
    // const notedIndex = note 
}